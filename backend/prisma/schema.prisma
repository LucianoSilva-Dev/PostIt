generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model PostIt {
  id        String   @id @default(uuid())
  text      String
  color     String
  textColor String
  createdAt DateTime @default(now())
}
